package com.GestionNote.exam.Repository.Oracle;

import com.GestionNote.exam.Entity.Oracle.Modulel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ModuleRepository extends JpaRepository<Modulel,String> {
}
